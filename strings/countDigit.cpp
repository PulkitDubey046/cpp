//Find the numbers of digits of the given number without using any loop.

#include<iostream>
#include<string>
using namespace std;
int main(){
    int x=1275;
    string s= to_string(x); //to changing into string
    cout<<s.length();
}